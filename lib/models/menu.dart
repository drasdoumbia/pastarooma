class Menu {
  final String title;
  final String description;
  final int points;
  final String image;
  final bool like;

  Menu({this.title, this.description, this.points, this.image, this.like = false});
}
